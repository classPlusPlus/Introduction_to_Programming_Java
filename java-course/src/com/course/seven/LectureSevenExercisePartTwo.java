package com.course.seven;

/**
 * In Java we always define a class 
 * Later we will discuss classes
 */
public class LectureSevenExercisePartTwo {
    /**
     * We define a main "function" later we will discuss more on functions
     * This is the entry point for a java application
     * We can run the main function 
     * We write our code inside the main function
     * public, static, void and String[] are "reserved" words that we don't use for variables
     * These terms will be discussed later
     */
    public static void main(String[] args) {
        int dimensionX;
        int dimensionY;

        // Ask the user to input dimensionX and dimensionY of a 2D array
        // Store the inputs in the variables above
        // add code here

        double[][] input;
        
        // Using loops ask the user to fill the 2D array
        // store values in the input variable
        // add code here

        double sum;
        
        // compute the sum of the 2D array input
        // print the sum
        // add code here
        
        double[] sumPerRow;

        // compute the sum of each row in the 2D array input
        // store in the 1D array sumPerRow
        // print the result
        // add code here

    }
}
